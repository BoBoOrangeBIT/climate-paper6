GLACE Nigeria — Part 4 of 4
Tracing and Adjudication
Steps 9 to 14 · authoritative sources, then the final adjudicated values
v1.0 · 2026-08-26 · Cell: NGA × split · Mirrors the Chinese master template v2.2 · Run this part alone, then stop
Preconditions
Parts 1 to 3 have been run and Checkpoint 1 has been approved by a human.
The records file, the run log, the snapshot manifest, the saved label images and the approved tracing worklist are all present.
This part does not revisit e-commerce listings except where a step says so explicitly.
Inputs
1. Role and constraints
You are tracing efficiency and capacity values to authoritative sources and adjudicating the final values. Retail values stay in the file as a comparison set and are never overwritten in place.
The final rated efficiency and rated capacity come from a registry or a manufacturer specification sheet. Retail values are used only when neither carries the model.
Record only what the source states. The single permitted computation is rated_eer, plus the two deviation columns in Step 13.
Never infer a missing value from a seasonal metric, a star rating or a nominal size class.
Match rates are the object of study, not a target. Do not raise a hit rate by loosening the match criteria.
At least three seconds between page loads, robots.txt respected, no CAPTCHA or login bypass. Where a bulk export exists, use it instead of querying record by record.
A source blocked to your network egress is recorded as gap code C-geo. Circumvention by proxy, VPN, mirror or cached copy is prohibited without exception.
Never delete a record because tracing failed. Leave the field blank, assign a gap code, and keep the row.
2. Step 9　Source inventory and reachability check
Open each source below and run one test query. Report reachability before proceeding.
Report which sources are reachable, which are blocked, and which offer a bulk export.
If the working tier order differs from T1a then T1b then T1c, state it explicitly before starting.
3. Step 10　Registry matching
manufacturer_mpn is the join key. It is the manufacturer model code and the only identifier registries carry.
platform_sku is not a model code. A platform item id is assigned by the platform, appears in no registry, and must never be used for matching. Colour and size variants of one model carry different platform_sku values, and sellers sometimes fill the model row of a specification table with one, so check the format before using the value.
Where the three model sources disagree, the label image wins, because the model printed on a label must match the registered record.
Where several candidates match and their efficiency values differ by more than five percent, do not pick one. Set match_ambiguous to true, leave the value blank, and list the candidates for human adjudication.
Suffix stripping rules are defined once for the run and written to the log. Do not invent a rule per record.
Order 3 hits very easily return several model years and efficiency tiers within one series, and the error is invisible in the data. Every order 3 hit carries match_ambiguous true.
A row with no recoverable model string is never deleted for failing to match. Such products skew toward Nigerian house brands and rebadged imports, so deletion manufactures non-random missingness.
4. Step 11　Manufacturer specification sheets and label images
Models the registry did not resolve go to the manufacturer own site. Priority 1 rows come here even when the registry supplied an efficiency value, if capacity or input power is still blank.
Only the manufacturer own pages and the PDFs they host qualify. Dealer pages, price comparison sites and third-party databases do not, however accurate they look.
Where a model has several market versions, take the Nigerian version. If it cannot be determined, set match_ambiguous to true and list the candidates.
A specification sheet is also the route to a missing rated capacity or input power, which is what decides whether rated_eer can be computed at all.
Where the registry and the manufacturer site both fail, fall back to the label images saved in Part 2 and set efficiency_source_tier to t1c_label_image. Do not return to the retail listing for this.
5. Step 12　Evidence capture
Save a full-page PNG of every source that produced a value; save hosted specification PDFs in their original form.
Capture evidence for hits only. Searches that found nothing get no snapshot; record in the log which sources were searched and how.
Naming: NGA_split_auth_{registry or manufacturer}_{manufacturer_mpn}_{YYYYMMDD}T{HHMMSS}Z.png
Compute SHA-256 into snapshot_auth_id and snapshot_auth_sha256.
efficiency_source_url now points at the authoritative source rather than the retail page. Each record thereby carries a price URL and an efficiency URL from different origins, which is the traceability property that distinguishes this dataset.
6. Step 13　Adjudication and deviation
Decide each field independently. Capacity and input power may come from different tiers as long as each is labelled honestly.
Fall back to tier 4 only when the first three carry nothing for that model, and record the fallback rather than hiding it.
Recompute rated_eer from the adjudicated capacity and input power, to two decimals.
Convert the authoritative capacity to watts at 0.29307 before comparing, so that a unit mismatch is never read as a real deviation.
Where retail and authoritative values both exist, compute capacity_deviation_pct and eer_deviation_pct against the authoritative value as denominator, to two decimals.
Deviations above ten percent in absolute value set qc_flag to large_deviation. Keep both numbers unchanged and refer them for human review.
Where the retail metric and the authoritative metric differ, compute no deviation and set qc_flag to metric_mismatch, recording both metric names.
The deviation distribution measures how wrong retail listings are. It is evidence for the technical validation section and is not to be trimmed because it looks poor.
7. Step 14　Fields and outputs
7.1　Tracing layer
7.2　Final layer
Column naming follows the existing file. Add the _auth columns, write adjudicated results into the unsuffixed columns, and keep the original retail values in _retail columns rather than renaming anything already collected.
7.3　Deliverables
Close with hit rates by tier, by brand tier and by pilot_segment, the fallback share to tier 4, and the count of deviations above threshold. Report the fallback share explicitly. In a market with a thin or absent registry it is the single most informative number in the run.
8. Nigeria rules that apply to this part
9. Pre-submission checklist
Checkpoint 1 was approved before this part began, and source reachability is reported with any blocked source recorded as C-geo and no circumvention attempted.
Priority 1 rows went through both the registry and the manufacturer site; none were skipped.
manufacturer_mpn was the join key throughout; platform_sku was never used for matching.
Model recovery was run only for rows with mpn_missing true, and mpn_recovered_from is filled for those rows.
Suffix stripping rules are defined once and logged.
Every value-producing source has a snapshot and a SHA-256; failed searches have none.
efficiency_source_url points at the authoritative source, not the retail page.
efficiency_source_tier and capacity_source are filled on every row, including fallbacks.
No EER value was substituted for NSEER, and metric mismatches are flagged rather than computed.
Capacity was converted to watts before any deviation was computed, and no horsepower value was converted.
rated_eer recomputed from adjudicated values; deviations computed and flagged where above threshold.
Hit rates reported by tier, by brand tier and by pilot_segment; the fallback share is reported explicitly.
No record was deleted because tracing failed.
On completion, deliver the three outputs and stop. Do not proceed to any cross-country aggregation.
--- TABLE 0 ---
Item | Content
Retail records file | The Part 3 output, keyed on manufacturer_mpn, including pilot_segment
Label images | Energy label images saved in Part 2, with their file names and hashes. Expected to be very few, since labelling is not yet enforced
Checkpoint 1 worklist | Priority 1 is rows whose rated cooling capacity or rated input power is blank, which have no fallback and where a tracing failure is a permanent hole. Priority 2 is rows with retail values present. Do not rebuild the worklist; deduplicate it on manufacturer_mpn, set tracing_priority on every row, and report any discrepancy against the approved list before starting
Country and metric | Nigeria / NGA. SON SONMELS under NIS 943:2024, one to five stars, native metric NSEER, not yet in force at the collection date
--- TABLE 1 ---
Tier | Source | Notes
T1a | SON, Standards Organisation of Nigeria, under SONMELS | The authority for NIS 943:2024. Certification is voluntary until the December 2026 deadline, so any register is expected to hold only early adopters. Establish whether a public model-level register exists and how far it reaches. If coverage is negligible, say so plainly and state the working tier order before starting.
T1a | Energy Commission of Nigeria | Policy-side documentation on the MEPS and the NSEER metric. Consult where SON is silent. Not a product register.
T1b | Manufacturer sites | LG, Samsung, Hisense, Midea, Gree, TCL, Haier, Daikin for international brands; Thermocool, Scanfrost, Nexus, Polystar, Royal, Bruhm, Century, Qasa, LONTOR for Nigerian brands. Locate each site by searching the brand name plus Nigeria rather than constructing a domain, and log the domain actually used. Official specification sheets and product pages only.
T1c | Label images from Part 2 | Already on disk. Use whatever was saved.
--- TABLE 2 ---
Order | Method | Result
0 | Model recovery, only for rows where mpn_missing is true: read the model code from the product title, the specification table, or the energy label image, in that order | Record mpn_recovered_from; on success return to order 1
1 | Exact match on manufacturer_mpn | Proceed to extraction
2 | Wildcard match after stripping colour codes, regional codes, model-year and packaging suffixes | Log the suffix rule used
3 | Brand plus the model fragment in the product name plus capacity class | Set match_ambiguous to true and record the candidate count
4 | No match | Gap code B, continue to Step 11
--- TABLE 3 ---
Priority | Source | capacity_source / efficiency_source_tier
1 | Registry | registry / t1_registry
2 | Manufacturer specification sheet | manufacturer_spec / t1_manufacturer_spec
3 | Official energy label image | label_image / t1c_label_image
4 | Retail listing | retail_page / t2_retail_claim
--- TABLE 4 ---
Field | Values and format | Required
efficiency_source_tier | t1_registry / t1_manufacturer_spec / t1c_label_image / t2_retail_claim / none | yes
mpn_recovered_from | title / spec_table / label_image / none; only where mpn_missing is true | conditional
tracing_priority | 1 or 2, per the Checkpoint 1 worklist | yes
registry_name | name of the register that produced the hit | conditional
registry_record_id | record identifier in the registry | conditional
registry_match_type | exact_mpn / wildcard_mpn / brand_model / none | yes
match_candidate_count | integer | conditional
efficiency_native | native efficiency value from the authoritative source | conditional
efficiency_metric | native metric name, verbatim, NSEER where stated | conditional
label_grade_auth | star rating recorded by the source, verbatim | conditional
label_version_auth | NIS 943:2024 or the edition stated; none where the source does not state it | conditional
capacity_raw_value_auth / capacity_raw_unit_auth | capacity figure and unit as printed by the source | conditional
rated_cooling_capacity_w_auth / rated_power_input_w_auth | numeric, watts | conditional
efficiency_source_url | URL of the authoritative source | yes
snapshot_auth_id / snapshot_auth_sha256 | snapshot file name and 64 hex characters | conditional
--- TABLE 5 ---
Field | Values and format | Required
rated_cooling_capacity_w | adjudicated value per Step 13 | yes
rated_power_input_w | adjudicated value per Step 13 | yes
capacity_source | registry / manufacturer_spec / label_image / retail_page | yes
rated_eer | adjudicated capacity divided by adjudicated input power, two decimals | yes
capacity_deviation_pct | retail minus authoritative, over authoritative, times 100, two decimals | conditional
eer_deviation_pct | same basis | conditional
qc_flag | blank / eer_out_of_range / large_deviation / metric_mismatch / unresolved_match | yes
match_ambiguous | true / false, already present in the Part 2 file; update it here | yes
--- TABLE 6 ---
File | Contents
{RUN_ID}_records.xlsx | The Part 3 record file with the tracing and final layers appended. Earlier columns unaltered
{RUN_ID}_run_log.md | The earlier log with two sections appended. Tracing section: source reachability, working tier order, suffix rules, sources searched and how, model recovery rate by source, hit rates by tier and by brand tier, hit rates by pilot_segment, fallback share, unresolved candidate list, deviations above threshold, elapsed time and tokens for this part. Defects section: registry behaviour anomalies, systematic gaps, situations this prompt does not cover
snapshots/auth/ | Authoritative source PNGs and PDFs with a sha256 manifest
--- TABLE 7 ---
Item | Rule
Pre-enforcement state | NIS 943:2024 is not yet in force at the collection date, so the registry layer is expected to be thin or empty and the manufacturer specification sheet is expected to carry the run. Report the T1a layer honestly, including a hit rate of zero if that is the result. A zero registry hit rate in a pre-enforcement market is a finding, not a failure.
NSEER | NSEER is a Nigerian seasonal metric and is not EER. Record it verbatim in efficiency_metric where a source states it. Never substitute an EER value for it, never back-derive rated_eer from it, and where the retail page reported EER and a source reports NSEER, set qc_flag to metric_mismatch and compute no deviation.
Pilot rows | Every row carries pilot_segment from Part 2. Trace calibration rows under exactly the same rules as production rows and report hit rates for the two segments separately as well as together, so the calibration subsample can be compared with the production subsample.
Capacity units | Nigerian sources state cooling capacity in BTU per hour. Record the printed figure and unit in the _auth raw columns, then convert at 0.29307 watts per BTU per hour. Horsepower is a nominal size class and is never converted.
Rebadged imports | Several Nigerian house brands are rebadged imports whose model codes resolve to no manufacturer site. A high fallback share for that tier is expected. Report it by brand tier rather than compensating for it, and never delete such rows for failing to match.
Regional variants | International brands sell Nigerian or wider African variants whose model codes differ from the global catalogue. Always take the Nigerian page where one exists. Where the site offers no country selector, set match_ambiguous to true and list the candidates.
